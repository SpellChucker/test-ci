version: '1.0'
steps:
  build_step:
    type: build
    image-name: SpellChucker/test-ci
    dockerfile: Dockerfile
    working_directory: ${{main_clone}}
    tag: ${{CF_BRANCH}}

  unit_tests:
    image: ${{build_step}}
    fail-fast: false
    commands:
      - npm test
